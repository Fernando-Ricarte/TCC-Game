#region CONTROLES

key_rigth = keyboard_check(ord("D")); // movimentação direita 
key_left = keyboard_check(ord("A")); //  movimentação esquerda
key_jump = keyboard_check(vk_space); // movimentação pulo
key_shoot = keyboard_check_pressed(ord("X")); // evento tiro
#endregion
